# YAHR runtime dependencies
a2a-sdk>=1.1.0
openai>=2.41.0
markitdown>=0.0.2
typer>=0.26.0
rich>=15.0.0
python-dotenv>=1.0.0

# A2A HTTP server transport (Starlette routes + ASGI server + SSE streaming)
starlette>=1.2.0
uvicorn>=0.49.0
sse-starlette>=3.4.0
