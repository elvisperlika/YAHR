# likec4 declares an optional peer on @tanstack/ai ^0.14, but this project uses
# @tanstack/ai 0.38 (via @tanstack/ai-openrouter). The peer is optional and only
# affects likec4's AI-chat panel, not diagram rendering, so accept the existing version.
legacy-peer-deps=true
